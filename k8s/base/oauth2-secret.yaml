# Secret pour OAuth2 Proxy - À créer avec les vraies valeurs
# kubectl create secret generic oauth2-secret \
#   --from-literal=CLIENT_SECRET=your-client-secret \
#   --from-literal=COOKIE_SECRET=your-cookie-secret \
#   -n todo-list
apiVersion: v1
kind: Secret
metadata:
  name: oauth2-secret
  namespace: todo-list
type: Opaque
# Remplacer par les vraies valeurs encodées en base64
# stringData:
#   CLIENT_SECRET: "your-client-secret"
#   COOKIE_SECRET: "your-cookie-secret"